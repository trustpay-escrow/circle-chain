## 📌 Pull Request Summary
Provide a high-level summary of the changes made in this PR and link related issues.

Fixes #(issue_number)

---

## 🛠️ Changes Implemented
- [ ] Added / updated feature logic in `frontend`, `backend`, or `contracts`.
- [ ] Updated corresponding documentation or schemas if applicable.

---

## ✅ Verification Checklist
- [ ] `npx tsc --noEmit` passes cleanly in `frontend` (if modifying frontend).
- [ ] `npx tsc --noEmit` passes cleanly in `backend` (if modifying backend).
- [ ] `cargo test` passes cleanly in `contracts` (if modifying Soroban smart contracts).
- [ ] Verified non-custodial security invariants (no private keys stored or transmitted).
